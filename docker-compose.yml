version: '3.8'

services:
  metadata-remover:
    build: .
    container_name: metadata-remover
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
